{   // !!! Not used in general. Settings.json is used when open a folder as workspace. But we open .code-workspace file, so the settings in that one is used.
    "git.autofetch": true,
    "git.enableSmartCommit": true,
    "files.exclude": {
        "**/*.js.map": true
    },
    "python.terminal.activateEnvironment": false,
    "html.format.wrapLineLength": 0,
    "diffEditor.ignoreTrimWhitespace": false
}